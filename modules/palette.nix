# Chọn theme đang dùng trong bộ sưu tập modules/themes/. Đây là nguồn màu
# CHUNG, DUY NHẤT của repo: kitty (modules/kitty.nix). Đổi theme: chỉ sửa dòng
# import bên dưới, KHÔNG cần đụng gì khác.
# KHÔNG viết mã hex ở bất kỳ file nào khác ngoài modules/themes/*.nix.
import ./themes/eldritch-dark.nix
